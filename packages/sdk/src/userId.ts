import { EthereumAddress } from '@streamr/utils'

// TODO will be UInt8Array
// TODO if we have this type in dht or trackerless-network package, we could just import it from there
export type UserID = EthereumAddress