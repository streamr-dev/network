// ITransport to WSConnector, create RPC stack in WSConnector
// Remove deferred, use existing IConnection, let handshake handle
// Remote logic in WSConnector

describe('WebSocket IConnection Requests', () => {
    it('happy path', () => {

    })
})