# Any tests that can run without building should go in here.
name: Client – Lint, Unit, Integration Tests

# Be sure to update both workflow files if you edit any env or trigger config
env:
  CI: true
  DEBUG_COLORS: true
  DEBUG: "Streamr*"
on:
  push:
    tags:
      - '*'
    branches:
      - main
  pull_request:
    branches:
      - '*'
  schedule:
    # run every day at 00:00
    - cron:  '0 0 * * *'

defaults:
  run:
    working-directory: packages/client

jobs:
  init:
    runs-on: ubuntu-latest
    steps:
      - name: Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.0
        with:
          all_but_latest: true
          access_token: ${{ github.token }}
  test:
    name: Test Unit using Node ${{ matrix.node-version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x, 14.x]
    env:
      TEST_REPEATS: 5
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
      - name: Cache Node.js modules
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.OS }}-node-${{ matrix.node-version }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.OS }}-node-${{ matrix.node-version }}
            ${{ runner.OS }}-node-
            ${{ runner.OS }}-
      - name: npm ci
        working-directory: './'
        run: |
          npm ci
          npx lerna bootstrap --scope streamr-client --include-dependencies
      - timeout-minutes: 2
        run: npx jest test/unit/AggregatedError.test.ts
      - timeout-minutes: 2
        run: npx jest test/unit/Connection.test.ts
      - timeout-minutes: 2
        run: npx jest test/unit/Encryption.test.ts
      - timeout-minutes: 2
        run: npx jest test/unit/GroupKeyPersistence.test.ts
      - timeout-minutes: 2
        run: npx jest test/unit/GroupKeyStore.test.ts
      - timeout-minutes: 2
        run: npx jest test/unit/iterators.test.js
      - timeout-minutes: 2
        run: npx jest test/unit/IteratorTest.js
      - timeout-minutes: 2
        run: npx jest test/unit/MessageCreator.test.ts
      - timeout-minutes: 2
        run: npx jest test/unit/PushQueue.test.js
      - timeout-minutes: 2
        run: npx jest test/unit/pUtils.test.js
      - timeout-minutes: 2
        run: npx jest test/unit/Scaffold.test.ts
      - timeout-minutes: 2
        run: npx jest test/unit/Session.test.ts
      - timeout-minutes: 2
        run: npx jest test/unit/Signer.test.ts
      - timeout-minutes: 2
        run: npx jest test/unit/Stream.test.ts
      - timeout-minutes: 2
        run: npx jest test/unit/StreamUtils.test.ts
      - timeout-minutes: 2
        run: npx jest test/unit/StubbedStreamrClient.ts
      - timeout-minutes: 2
        run: npx jest test/unit/utils.test.ts
